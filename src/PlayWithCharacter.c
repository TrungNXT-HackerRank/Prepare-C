#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    char ch;
    char s[100];
    char sen[100];
    scanf("%ch", &ch);
    scanf("%[^\n]%*c", &s);
    scanf("\n", &sen);
    printf("%ch", ch);
    printf("%s");
    printf("%sen");
    return 0;
}